---
title: "リレー/BP接続設定"
---

![リレーとBPの関係図](/images/producer-relay-diagram.png)

<Callout type="info" title="リレーとBPの役割">


- **リレー** … BP とネットワークの橋渡し。ブロック同期・伝播を行う。
- **BP** … ブロック生成専用。`node.cert` / `kes.skey` / `vrf.skey` で起動し、**自リレーのみ**に接続する。

</Callout>


---

## 1. P2P接続とトポロジーの理解 [#sec-1]

P2P では接続先の多くはノードが自動で選びます。  
**必ずつなぎたい相手（自BP・自リレーなど）だけ** `localRoots` に書きます。

### 接続状態（Hot / Warm / Cold） [#sec-2]

ノードは接続先を次の3種類で管理します。

| 状態 | 説明 |
| --- | --- |
| **Hot** | ブロック同期・伝播に使っているアクティブな接続 |
| **Warm** | 接続は維持し、必要時に Hot に昇格する待機接続 |
| **Cold** | 候補として保持しているが、まだ接続していないピア |

**覚えておくとよいこと**

- `advertise`は`Peer Sharing`でその接続先を他ノードへ知らせてよいかを表します。  
- BPや内部用リレーなど、外部へ公開したくない接続先は `false`に設定。

---

## 2. トポロジーファイルの変更 [#sec-3]

<Callout type="info" title="topology.json">


- **リレー** … 自BP・他リレーなど、固定したい相手を `localRoots` に記載。
- **BP** … **自リレーだけ**。`useLedgerAfterSlot` は **`-1`**（台帳ピアを使わない）。
- **IP** … サーバーの**パブリック（固定）アドレス**。ポートはノード用（SSH と混同しない）。

</Callout>


同期が 100% になってから以降の手順を実施してください。

```bash
cardano-cli latest query tip ${NODE_NETWORK} | grep syncProgress
```
```yaml title="期待される表示例：" noCopy
"syncProgress": "100.00"
```


<Callout type="error" title="リレー">


行末の「`＋`」で注釈を表示できます。


<AnnotatedCode
  code={[
    'cat > ${NODE_HOME}/topology.json << EOF',
    '{',
    '  "bootstrapPeers": [',
    '    {',
    '      "address": "leios-node.play.dev.cardano.org",',
    '      "port": 3001',
    '    },'
    '  ],',
    '  "localRoots": [',
    '    {',
    '      "accessPoints": [',
    '        {',
    '          "address": "BPのIP",',
    '          "port": 3010',
    '        }',
    '      ],',
    '      "advertise": false,',
    '      "trustable": true,',
    '      "valency": 1',
    '    }',
    '  ],',
    '  "peerSnapshotFile": "peer-snapshot.json",',
    '  "publicRoots": [],',
    '  "useLedgerAfterSlot": 100',
    '}',
    'EOF',
  ].join('\n')}
  annotations={{
    21: 'BP の IP。',
    25: 'BP 宛の localRoot は必ず `advertise: false` とします。',
  }}
/>


</Callout>

<Callout type="error" title="BP">
行末の「`＋`」で注釈を表示できます。

<AnnotatedCode
  code={[
    'cat > ${NODE_HOME}/topology.json << EOF',
    '{',
    '  "bootstrapPeers": null,',
    '  "localRoots": [',
    '    {',
    '      "accessPoints": [',
    '        {',
    '          "address": "リレー1のIP",',
    '          "port": 3010',
    '        }',
    '      ],',
    '      "advertise": false,',
    '      "trustable": true,',
    '      "valency": 1',
    '    }',
    '  ],',
    '  "publicRoots": [],',
    '  "useLedgerAfterSlot": -1',
    '}',
    'EOF',
  ].join('\n')}
  annotations={{
    8: 'リレー1 の IP アドレス',
    22: '`-1` で台帳ピアを使わない BP 向けモードになります。',
  }}
/>

</Callout>


### 構文チェックと再起動 [#sec-6]

```bash
jq . $NODE_HOME/topology.json
```

JSON がそのまま表示されれば問題ありません。  
`parse error` のときは `{}` `[]` `,` を確認してください。

設定ファイル再読み込み
```bash
cnreload
```

## 3. BPをブロック生成モードへ切替 [#sec-3]

最後に、BP ノードを KES / VRF / 運用証明書を指定して起動するよう切り替えます。

ノードを停止します。
```bash
sudo systemctl stop cardano-node
```

起動スクリプトにブロック生成キーを追記して更新します。

```bash
cat > $NODE_HOME/startNode.sh << EOF
#!/bin/bash
DIRECTORY=${NODE_HOME}
PORT=3010
HOSTADDR=0.0.0.0
TOPOLOGY=\${DIRECTORY}/topology.json
DB_PATH=\${DIRECTORY}/db
SOCKET_PATH=\${DIRECTORY}/db/socket
CONFIG=\${DIRECTORY}/config.json
KES=\${DIRECTORY}/keys/kes.skey
VRF=\${DIRECTORY}/keys/vrf.skey
CERT=\${DIRECTORY}/keys/opcert.cert
${HOME}/.nix-profile/bin/cardano-node run --topology \${TOPOLOGY} --database-path \${DB_PATH} --socket-path \${SOCKET_PATH} --host-addr \${HOSTADDR} --port \${PORT} --config \${CONFIG} --shelley-kes-key \${KES} --shelley-vrf-key \${VRF} --shelley-operational-certificate \${CERT}
EOF
chmod +x $NODE_HOME/startNode.sh
```

ノードを起動します。
```bash
sudo systemctl start cardano-node
```

ログでブロック生成ノードとして起動しているか確認します。
```bash
journalctl -u cardano-node -f
```

<Callout type="error" title="ブロック生成に必要な3ファイル">

BP でブロックを生成するには以下の3ファイルが必要です。起動時に指定されていない場合はブロックを生成できません。

- `kes.skey`
- `vrf.skey`
- `opcert.cert`

</Callout>

---