---
title: "リレーノード構築(Nix)"
---

リレーノードは **Nix** を使用して構築します。

<Callout type="info" title="リレーでNixを使う理由">

- **テストネットのため、仕様やバージョンが都度変更される可能性がある** … Nix なら `nix run github:input-output-hk/ouroboros-leios#leios-testnet-relay` を再実行するだけで、最新の構成（ノード・設定・監視スタック）に追従できます。
- **セットアップ作業工数の軽減** … 依存関係のインストールやソースからのビルドが不要で、ノードと監視スタックをワンコマンドで一括起動できます。

</Callout>


<Callout type="info" title="このページで行うこと">

1. Nix のインストールと flakes の有効化
2. 作業ディレクトリの準備
3. リレーノードの起動（動作確認）
4. tmux による常駐化
5. 同期状況の確認

</Callout>


## 1. Nixのインストール [#sec-1]

Nix パッケージマネージャをインストールします。

```bash
curl --proto '=https' --tlsv1.2 -L https://nixos.org/nix/install | sh -s -- --daemon
```

セッションの再起動
```
exec $SHELL
```

インストールを確認します。
```bash
nix --version
```
```yaml title="期待される表示例：" noCopy
nix (Nix) 2.x.x
```


## 2. flakesの有効化 [#sec-2]

Leios のコマンドは flakes 機能を利用するため、有効化します。

```bash
echo "experimental-features = nix-command flakes" | sudo tee -a /etc/nix/nix.conf
echo "accept-flake-config = true" | sudo tee -a /etc/nix/nix.conf
echo "trusted-users = $(whoami)" | sudo tee -a /etc/nix/nix.conf
sudo systemctl restart nix-daemon
```

<Callout type="info" title="trusted-users の設定について">

`trusted-users` を設定しないと、IOG のバイナリキャッシュ（`cache.iog.io`）が利用されず、`you are not a trusted user` という警告とともに、すべてをソースからビルドすることになり非常に時間がかかります。  
必ず設定し、`nix-daemon` を再起動してください。

</Callout>


## 3. 作業ディレクトリの準備 [#sec-3]

作業ディレクトリを環境変数に設定します。  
`WORKING_DIR` には、ノードのDBや設定ファイル、ソケットが配置されます。

```bash
echo 'export WORKING_DIR=$HOME/cnode' >> $HOME/.bashrc
echo 'export NODE_HOME=$HOME/cnode' >> $HOME/.bashrc
echo 'export CARDANO_NODE_NETWORK_ID=164' >> $HOME/.bashrc
echo 'export CARDANO_NODE_SOCKET_PATH=$HOME/cnode/db/socket' >> $HOME/.bashrc
source $HOME/.bashrc
mkdir -p "$NODE_HOME"
```

<Callout type="info" title="設定ファイルについて">

リレーは `nix run github:input-output-hk/ouroboros-leios#leios-testnet-relay` を使用します。  
このコマンドが **ノード・設定ファイル・監視スタック（Grafana / Prometheus / Loki）を一括で用意し、`$NODE_HOME` 配下に配置** するため、設定ファイルを手動で取得する必要はありません。

</Callout>


## 4. リレーノードの起動（動作確認）[#sec-4]

まずはワンコマンドでリレーを起動し、正常に動作するか確認します。

```bash
tmux new -s relay
```

```bash
cd $home
nix run github:input-output-hk/ouroboros-leios#leios-testnet-relay
```

<Callout type="info" title="初回起動について">

- 初回はバイナリのダウンロード・ビルドに時間がかかる場合があります。
- 起動時に `allow-import-from-derivation` や `extra-substituters` の許可を求められた場合は `y` を入力してください。
- ノードに加えて監視用のダッシュボード（Grafana）が `http://localhost:3000` で起動します。

</Callout>

セッションから抜ける（デタッチ）には、`Ctrl + b` を押してから `d` を押します。  
> デタッチしてもリレーは **バックグラウンドで動作し続けます。** SSH接続を切断しても停止しません。

再度ダッシュボードを表示する（アタッチ）には次を実行します。
```bash
tmux a -t relay
```

## 5. cardano-node / cardano-cliの固定インストール [#sec-5]

`dev-testnet` シェルが提供する `cardano-node` / `cardano-cli` の **Nix store 上の実体パス**を取得します。

```bash
NODE_STORE=$(nix develop github:input-output-hk/ouroboros-leios#dev-testnet \
  --command bash -c 'dirname "$(dirname "$(readlink -f "$(command -v cardano-node)")")"')
```
<Callout type="info" title="初回実行時">
nixキャッシュダウンロードのため初回実行時は数分かかります
</Callout>

```bash
CLI_STORE=$(nix develop github:input-output-hk/ouroboros-leios#dev-testnet \
  --command bash -c 'dirname "$(dirname "$(readlink -f "$(command -v cardano-cli)")")"')
```
```bash
echo "cardano-node: $NODE_STORE"
echo "cardano-cli : $CLI_STORE"
```
```yaml title="期待される表示例：" noCopy
cardano-node: /nix/store/xxxx...-cardano-node-...
cardano-cli : /nix/store/yyyy...-cardano-cli-...
```

取得した store パスを `nix profile install` でプロファイルに固定インストールします。

```bash
nix profile install "$NODE_STORE"
[ "$CLI_STORE" != "$NODE_STORE" ] && nix profile install "$CLI_STORE"
```

<Callout type="info" title="このコマンドの効果">

- `~/.nix-profile/bin/cardano-node` / `~/.nix-profile/bin/cardano-cli` という**安定したパス**が作成されます。
- プロファイルには **gc-root** が張られるため、`nix-collect-garbage` でバイナリが削除されません。
- systemd からはこの固定パスを参照します（`nix develop` を実行時に評価しません）。

</Callout>


バージョンを確認します。

```bash
cardano-node --version
cardano-cli --version
```
```yaml title="期待される表示例：" noCopy
cardano-node 11.0.1.164 - linux-x86_64 - ghc-9.6  
git rev db5373ea6a6f79b398b468d2e04ce43eafcf90d0  
> 末尾に `164` が含まれていれば Leios プロトタイプ用ビルドです。

cardano-cli 11.0.0.0 - linux-x86_64 - ghc-9.6  
git rev db5373ea6a6f79b398b468d2e04ce43eafcf90d0  
```

## 6. gLiveViewのインストール [#sec-6]

`cardano-node` はログが流れる画面だけでは状態が分かりにくいため、ノードの状態を視覚的に確認できる `gLiveView` を導入します。

> `gLiveView` は [Guild Operators](https://cardano-community.github.io/guild-operators/#/Scripts/gliveview) によるツールで、systemd サービスと連携してノードのステータスを表示します。

必要な依存パッケージをインストールし、スクリプト用ディレクトリを作成します。
```bash
mkdir -p $NODE_HOME/scripts
cd $NODE_HOME/scripts
sudo apt install bc tcptraceroute -y
```

`gLiveView` 本体と `env` ファイルをダウンロードします。
```bash
curl -s -o gLiveView.sh https://raw.githubusercontent.com/cardano-community/guild-operators/master/scripts/cnode-helper-scripts/gLiveView.sh
curl -s -o env https://raw.githubusercontent.com/cardano-community/guild-operators/master/scripts/cnode-helper-scripts/env
chmod 755 gLiveView.sh
```

`env` ファイルの定義を、本マニュアルの構成（ポート `3010`）に合わせて修正します。
```bash
sed -i $NODE_HOME/scripts/env \
    -e '1,73s!#CNODE_HOME="/opt/cardano/cnode"!CNODE_HOME="'${NODE_HOME}'"!' \
    -e '1,73s!#CNODE_PORT=6000!CNODE_PORT=3010!' \
    -e '1,73s!#UPDATE_CHECK="Y"!UPDATE_CHECK="N"!' \
    -e '1,73s!#CONFIG="${CNODE_HOME}/files/config.json"!CONFIG="${CNODE_HOME}/config.json"!' \
    -e '1,73s!#SOCKET="${CNODE_HOME}/sockets/socket"!SOCKET="${CNODE_HOME}/db/socket"!' \
    -e '1,73s!#PROM_HOST=127.0.0.1!PROM_HOST=127.0.0.1!' \
    -e '1,73s!#PROM_PORT=12798!PROM_PORT=12798!'
```

<Callout type="info" title="修正される定義">

| 項目 | 値 |
| :---- | :---- |
| `CNODE_HOME` | `$NODE_HOME`（`$HOME/cnode`） |
| `CNODE_PORT` | `3010` |
| `CONFIG` | `${CNODE_HOME}/config.json` |
| `SOCKET` | `${CNODE_HOME}/db/socket` |
| `PROM_PORT` | `12798` |

</Callout>

任意の単語で起動できるようエイリアスを登録します。
```bash
echo "alias glive='cd $NODE_HOME/scripts; ./gLiveView.sh'" >> $HOME/.bashrc
source $HOME/.bashrc
```


`gLiveView` を起動します。
```bash
glive
```

![gLiveView](/images/glive-leios.png)


## 7. Grafana設定 [#sec-7]

> ファイアウォールを設定している場合は3000ポートを解放してください

<Callout type="info" title="ファイアウォールについて">

VPS側でハードウェアファイアウォールを設定している場合はufwを使用しないでください

</Callout>

```
sudo ufw allow 3000/tcp
sudo ufw reload
```

nix側のGrafana設定ファイルパス検出
```
GRAFANA_INI=$(ps aux | grep '[g]rafana' | grep -oP '(?<=--config\s)[^ ]+')
```

外部アクセス許可
```
sudo sed -i 's/enforce_domain\s*=\s*true/enforce_domain=false/g' "$GRAFANA_INI"
```


nixシェル画面でGrafanaを再起動(Restart)する

```
tmux a -t relay
```
![nix shell](/images/grafana-restart.png)


ローカルPCのブラウザから`http://<サーバーIP>:3000` で Grafana ダッシュボードにアクセスできます。  



<Callout type="warn" title="サーバー再起動時の自動起動について">

tmux セッションは **サーバー再起動では復元されません。** 再起動後は再度 `tmux new -s relay` → `nix run ...` で起動してください。  
**完全な自動起動が必要で、対話ペインが不要な場合**は、process-compose を非対話モード（TUI無効）で systemd 登録する方法もあります。  
ただしその場合、ペイン画面は使えないため、状態確認は **Grafana（`:3000`）** や下記の `cardano-cli query tip` を使用してください。

</Callout>




---
